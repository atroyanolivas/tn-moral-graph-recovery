[tool.setuptools.packages.find]
include = ["tn_causal*"]

[project]
name = "tn-for-nn-explicability"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "numpy",
    "opt_einsum",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
